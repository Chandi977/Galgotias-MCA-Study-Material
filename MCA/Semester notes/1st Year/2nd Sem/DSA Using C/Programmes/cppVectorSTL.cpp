#include<bits/stdc++.h>
using namespace std;
int main(){
    // int basic[3] = {1,2,3};
    // array<int,4> a = {1,2,3,4};
    // int size = a.size();
    // for(int i = 0; i <size; i++){
    //     cout<<a[i]<<" ";
    // }
    // cout<<endl;
    // cout<<a.at(2)<<endl;
    // cout<<a.front()<<endl;
    // cout<<a.back()<<endl;
    // cout<<a.empty()<<endl;

    // Vector iin cpp
    /*
    vector is dynamically created array or storage in the run time or execution of a program.
    Size or vector can be manipulate any time of the program.
    Size can be increase or Decrease in the run time.
    vector is written as "vector<int>v";
    */


    // vector<int>v;

    // // Predefined vector size
    // // vector<int> vector_name(size,elements..);
    // vector<int> a(5,1);
    // for(int i:a){
    //     cout<<i<<" ";
    // }
    // cout<<endl;
    // cout<<"Size of vector v is :"<<v.capacity()<<endl;

    // // Insertion in vector using Push operation.
    // v.push_back(1);
    // cout<<"Size of vector v is :"<<v.capacity()<<endl;
    // v.push_back(2);
    // cout<<"Size of vector v is :"<<v.capacity()<<endl;
    // v.push_back(3);
    // cout<<"Size of vector v is :"<<v.capacity()<<endl;
    // v.push_back(4);
    // cout<<"Size of vector v is :"<<v.capacity()<<endl;
    // // If the vector size has reached then it will allocate its memory into 2 times that are already has a size of
    // // eg if v vector is empty the capacity shows 0
    // // then insert an element 1 the capacity increased to 1
    // // after that again an element inster into v the capacity shows 2
    // // again doing the same process capacity will increase into 2 time that is 4


    // // Printing the values of V
    // cout<<"Before POP, all Element of vector v : "<<endl;
    // for(int i = 0; i < v.size(); i++){
    //     cout<<v[i]<<" ";
    // }
    // cout<<endl;

    // // Deletion in Vector using POP function
    // v.pop_back(); //delete an element from end to array or last index of array.
    // cout<<"After Deletion an element from end or back from vector v."<<endl;
    // for(int i = 0; i < v.size(); i++){
    //     cout<<v[i]<<" ";
    // }
    // cout<<endl;
    // cout<<"Again deleting the element from end "<<endl;
    // v.pop_back();
    // for(int i = 0; i < v.size(); i++){
    //     cout<<v[i]<<" ";
    // }
    // cout<<endl;
    
    // // Copy vector into another.
    // cout<<"Copy a vector into another. "<<endl;
    // vector<int>copy_vec(a);
    // for(int i:copy_vec){
    //     cout<<i<<" ";
    // }
    // cout<<endl;


    // Double Ended Queue
    /*
    double ended queue can insert or delete from front or end.
    it store data into different array location and can be stored their address.
    representing as "deque<int>a";
    */
    deque<int>a;

    //insertion and deletion are same as vector insertion and deletion .
    
}